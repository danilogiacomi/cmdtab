name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test-lint:
    name: Build · Test · Lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: pin the Swift / Xcode toolchain once chosen, e.g.:
      # - uses: maxim-lobanov/setup-xcode@v1
      #   with: { xcode-version: latest-stable }

      - name: Build
        run: |
          # TODO: replace with the real build command once a manifest exists.
          echo "TODO: swift build  (or: xcodebuild -scheme CmdTab build)"

      - name: Test
        run: |
          # TODO: replace with the real test command.
          echo "TODO: swift test  (or: xcodebuild -scheme CmdTab test -destination 'platform=macOS')"

      - name: Lint
        run: |
          # TODO: install + run swiftlint / swift-format.
          echo "TODO: swiftlint && swift format lint --recursive Sources"
